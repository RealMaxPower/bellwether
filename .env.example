# Copy to .env.local and fill in. Only needed to run `npm run refresh-data`
# (the app serves checked-in JSON at runtime and does NOT require this key).
# Get a free key: https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY=
